using UnityEngine;

public class DayUI : MonoBehaviour
    //cod pentru a putea vedea in calendar doar ultimele 30 de zile, restul nu, fie butonul stang/drept devine inactiv
    //fie zilele devin gri
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
